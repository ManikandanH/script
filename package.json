{
  "name": "load",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "export NODE_OPTIONS=--experimental-vm-modules NODE_NO_WARNINGS=1 && node index.js",
    "sg": "export NODE_OPTIONS=--experimental-vm-modules NODE_NO_WARNINGS=1 && node catalogue.js"
  },
  "type": "module",
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.2.3",
    "node-fetch": "^3.3.0"
  }
}
